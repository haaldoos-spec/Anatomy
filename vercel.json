{
  "framework": "nextjs",
  "installCommand": "npm install --ignore-scripts && cd backend && npm install && cd ..",
  "buildCommand": "next build",
  "outputDirectory": ".next",
  "env": {
    "NEXT_PUBLIC_API_URL": "@next_public_api_url"
  }
}
